# Google Chrome

Google Chrome is a vendor browser package, not an LFS-native source build.

Keep it as a direct package only after license and redistribution review. MEMO should prefer `chromium` for source-first official builds.

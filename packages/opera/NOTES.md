# Opera

Opera is a direct vendor browser package. It needs redistribution review before it can be shipped by MEMO.

Use this recipe only for reviewed vendor payload staging; do not present it as a normal source build.
